// Ternary Operators
// syntax --> condition ? true : false

let age = 25;

let result = age > 18 ? "adult" : "not adult";
console.log(result)

// it is a compact type if if-else
// it is best for simple two-way decisions 